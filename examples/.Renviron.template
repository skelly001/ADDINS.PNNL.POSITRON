# .Renviron configuration for sandbox-sync
# Place this file in your SCRIPT_PATH (r_script_sandbox root)

# REQUIRED: Absolute path to your scripts sandbox (Git-tracked)
# This should be the root of your r_script_sandbox directory
SCRIPT_PATH="/path/to/r_script_sandbox"

# REQUIRED: Absolute path to your data sandbox (OneDrive-backed)
# This should be the root of your data_sandbox directory
DATA_PATH="/path/to/data_sandbox"

