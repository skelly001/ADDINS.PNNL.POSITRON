# Docker Environment Configuration for Sandbox-Sync Development
# Copy this file to .env and customize for your system

# ============================================================================
# Sandbox Paths
# ============================================================================
# Windows paths - Docker will handle path conversion in WSL2
# Adjust these to match your actual development directories

# Scripts sandbox (Git-tracked)
SCRIPT_PATH=C:\\Users\\kell343\\dev\\r_script_sandbox2_vscode_test

# Data sandbox (OneDrive, no Git)
DATA_PATH=C:\\Users\\kell343\\OneDrive - PNNL\\Documents\\data_sandbox2_vscode_test

# Alternatively, use test directories (default):
# SCRIPT_PATH=./test-scripts
# DATA_PATH=./test-data

# ============================================================================
# User Configuration (Linux/macOS only)
# ============================================================================
# Match host user UID/GID for seamless file permissions
# On Windows with WSL2, use default values (1000)
# On Linux/macOS, uncomment and set to your actual UID/GID

# USER_UID=1000
# USER_GID=1000

# To get your UID/GID on Linux/macOS, run:
#   id -u  (for UID)
#   id -g  (for GID)

# ============================================================================
# Development Settings
# ============================================================================
# Log level for sandbox-sync binary (debug, info, warn, error)
LOG_LEVEL=info

# Build mode (debug or release)
BUILD_MODE=release

# Enable cross-compilation builds
CROSS_COMPILE=false

# Clean build (removes previous artifacts)
CLEAN_BUILD=false

# Create distribution package
CREATE_DIST=false

# ============================================================================
# Optional: Additional Configuration
# ============================================================================
# Workspace root (usually don't need to change)
# WORKSPACE_ROOT=/workspace

# Node environment
# NODE_ENV=development

# Rust backtrace (1 for backtrace, full for detailed backtrace)
# RUST_BACKTRACE=1
